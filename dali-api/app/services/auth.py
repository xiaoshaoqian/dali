"""Authentication service placeholder."""

# Future implementation:
# - send_sms_code(phone: str) -> bool
# - verify_sms_code(phone: str, code: str) -> User
# - create_tokens(user: User) -> TokenPair
# - refresh_access_token(refresh_token: str) -> TokenPair
# - logout(user_id: str) -> None
