"""Alibaba Cloud OSS integration placeholder.

Used for image storage with SSE encryption.
"""

# Future implementation:
# - generate_presigned_upload_url(key: str, expires: int) -> str
# - generate_presigned_download_url(key: str, expires: int) -> str
# - delete_object(key: str) -> bool
# - list_objects(prefix: str) -> list[str]
